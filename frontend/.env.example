# Same-origin API via Vite dev proxy (one URL for localhost + Cloudflare tunnel)
VITE_API_BASE=/api

# Optional: backend target for Vite proxy (default 127.0.0.1:8000)
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
